# ⚠️ Conversion Skipped
# The provided file appears to be React or frontend TypeScript/JSX code.
# Such files depend on the browser (DOM, JSX, hooks, etc.) and cannot be meaningfully converted to Python.
# If you want to convert only utility logic, extract it into a plain TypeScript function first.
